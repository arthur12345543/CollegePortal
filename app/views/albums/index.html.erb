<div style="text-align: right;">


<% if user_signed_in? %>
	<%= link_to "Создать альбом", new_albums_path %>	
<% else %>
	Для того чтобы создать альбом вам необходимо зайти на сайт
<%end%>


</div>


<h1><center><a href="" target="_blank">Название альбома</a></center></h1>
<div id="photogaleleryIW">
	<img src="" id="photogaleleryI" />
</div>



<h1>Listing posts</h1>
 
<table>
 
<% @albums.each do |albums| %>
  <tr>
  	<td><%= albums.id %></td>
    <td><%= albums.title %></td>
    <td><%= albums.user_id %></td>
    <td><%= link_to 'Show', albums %></td>
    <td><%= link_to 'Edit', "#" %></td>
    <td><%= link_to 'Destroy', "#", :confirm => 'Are you sure?',
                                     :method => :delete %></td>
  </tr>
<% end %>
</table>
 
<br />