<%= form_for :lesson, :url => { :action => :replacement_save, :id => @lesson.id },
:method => :put do |f| %>
  <% if @lesson.errors.any? %>
  <div id="errorExplanation">
    <h2><%= pluralize(@lesson.errors.count, "error") %> ошибок возникло при сохранении изменений:</h2>
    <ul>
    <% @lesson.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
  <% end %>
  Предмет:<br>
  <%= f.text_field :replacement_lesson %><br>
  Кабинет:<br>
  <%= f.text_field :replacement_room %><br>
  Преподователь:<br>
  <%= f.text_field :replacement_teacher %><br>


<%= f.submit 'сохранить' %>
  </p>
<% end %>
 
<%= link_to 'Back', :action => :index %> 
