<%= form_for :lesson, :url => { :action => :update, :id => @lesson.id },
:method => :put do |f| %>
  <% if @lesson.errors.any? %>
  <div id="errorExplanation">
    <h2><%= pluralize(@lesson.errors.count, "error") %> ошибок возникло при сохранении изменений:</h2>
    <ul>
    <% @lesson.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
  <% end %>
  Предмет (если имеется мигалка, то используется для чётного урока):<br>
  <%= f.text_field :even_lesson %><br>
  Кабинет (если имеется мигалка, то используется для чётного урока):<br>
  <%= f.text_field :even_room %><br>
  Преподователь (если имеется мигалка, то используется для чётного урока):<br>
  <%= f.text_field :even_teacher %><br>
  Предмет в нечётные дни (для мигалки)<br>
  <%= f.text_field :odd_lesson %><br>
  Кабинет в нечётные дни (для мигалки):<br>
  <%= f.text_field :odd_room%><br>
  Преподователь в нечётные дни (для мигалки):<br>
  <%= f.text_field :odd_teacher %><br>
  День является мигалкой
  <%= f.check_box :double_lesson %> <br>





<%= f.submit 'сохранить' %>
  </p>
<% end %>
 
<%= link_to 'Back', :action => :index %> 
