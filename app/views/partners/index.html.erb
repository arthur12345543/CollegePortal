<br>
<div style="min-width: 1300px; text-align: center;"><h1 style="color: #000; text-align: center;">Партнёры</h1></div>
<% if current_user && current_user.role && current_user.role.can_edit_partners%>
<%= link_to 'Создать', 	new_partner_path %>
<%else%>
<br>
<%end%>

<% @Partners.each do |n| %>
<div class="bord">
  
  <table width='100%' border='0' >
  <tr>
  <td rowspan='2' width='150' align='center'>
  <%= image_tag n.picture%>
  </td>
  <td>
  <center><%= n.name  %></center>
  </td>
  
  </tr>
  <tr>
  <td>
  <%= n.text %>
  </td>
  </tr>
  <tr>
  <td>
  </td>
  <td>
  <% if current_user && current_user.role && current_user.role.can_edit_partners%>
  <%= link_to 'удалить', { :action => :destroy, :id => n.id }, :method => :delete, :confirm => 'Are you sure?' %>
  <%end%>
  </td>
  </tr>
  </table>
  </div>
  <%end%>

 
